<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.ethings.com.ethingsble">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity  android:name="com.ethings.ble.activity.SplashActivity"
            android:noHistory="true"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.ethings.ble.activity.AddNewTaskActivity" />
        <activity android:name="com.ethings.ble.activity.ListTagActivity" />
        <activity
            android:name="com.ethings.ble.activity.MainActivity"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>